# User Data

This folder documents the shape of personal learning state.

Real files such as `progress.yaml`, `mistakes.yaml`, and `review-queue.yaml` are gitignored by default because the repository may be public.

Use `example-progress.yaml` as a template.
